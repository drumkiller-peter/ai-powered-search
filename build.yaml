targets:
  $default:
    builders:
      ferry_generator|graphql_builder:
        enabled: true
        options:
          schemas:
            - ai_powered_search|lib/api/graphql/schema.graphql
          type_overrides:
            Long:
              name: int
            LibraryCollectionTypeEnum:
              name: String
            ListBuilder:
              name: List    
      ferry_generator|serializer_builder:
        enabled: true
        options:
          schemas:
            - ai_powered_search|lib/api/graphql/schema.graphql
